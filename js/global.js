var RawHydeConfig = {
	disqusShortname: '',
	googleAnalyticsId: ''
};

(function( doc ) {
	if( !RawHyde.isEnhanced() ) {
		return;
	}

	// User JavaScript goes here
	// Inject a web font, add some icons
})( document );